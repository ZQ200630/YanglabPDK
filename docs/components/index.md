# Component Reference

This section is generated from the `components/` source tree.  It is intended
to work like a local PCell catalog: use it to find function names, parameters,
module paths, docstrings, and available preview images.

Regenerate it with:

```powershell
python scripts/generate_component_docs.py
```

```{toctree}
:maxdepth: 1

../generated/components
```

See the generated [component report](../generated/component_report.md) for
preview status and skipped components.
